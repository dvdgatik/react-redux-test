// value default is zero 'cause the function return an id
const defalutState = 0;

// second parameter is action we are using  object destructuration
function reducer(state = defaultState, {type, payload}) {
	switch(type) {
		default:
			return state;

	}

}


export default reducer;